# 项目开发路线图 (Roadmap)

## 📅 计划中

- [ ] **Docker 容器化部署支持**
    - [ ] **多阶段构建与制品编译 (Multi-stage Build)**
        - [ ] **前端资产编译阶段**：基于 Node.js 生产环境镜像，执行前端 (Web SPA) 的依赖安装与静态资源构建，输出优化后的 Production Ready 静态制品。
        - [ ] **后端服务编译阶段**：执行服务端 (Server) 的依赖安装，通过 TypeScript 编译器 (tsc) 将源码编译为高执行效率的 ESM 目标代码。
    - [ ] **单镜像运行态收敛 (Static hosting & Unified Entry)**
        - [ ] 将前端编译产物 (dist/public) 物理集成至后端 Express 服务的静态资源托管目录中。
        - [ ] 容器运行时仅暴露单个业务端口（默认 `3000`），承载 SPA 路由分发、`/internal` 内部管理接口、`/api` 对外数据接口以及 `/api/mcp` 端点服务。

- [ ] **NPM 发布与本端免部署运行支持 (NPX Engine)**
    - [ ] **零配置免安装执行器开发 (CLI Bootstrapper)**
        - [ ] 构建专用的全局命令行工具（支持通过 `npx` 管道即时免安装运行）。
        - [ ] 支持通过命令行入参（例如 `--config <path>`）载入标准 JSON 格式的网关配置文件。
        - [ ] 依据载入的配置文件，自动实例化底层的路由总线并动态拉起相应的 MCP Server 实例。
    - [ ] **图形化配置透出与多拓扑导出引擎 (Profile Export Utility)**
        - [ ] **全栈系统配置导出组件**：在前端管理后台中集成配置快照导出（JSON Schema 级联校验）能力。
        - [ ] **按需按域导出机制**：支持用户选择性勾选特定 API 项目集进行打包导出（自动执行前置校验，剔除未激活 MCP 能力的非必要项目）。
        - [ ] **多环境存储策略适配 (Storage Layer Sandbox)**：
            - **临时沙箱模式 (Ephemeral Mode)**：在系统临时目录 (OS Temp Directory) 中创建隔离的、生命周期随进程销毁的临时数据根目录。
            - **持久化沙箱模式 (Persistent Local Mode)**：采用符合主流操作系统规范的应用数据存储路径（例如：Windows 的 `%LOCALAPPDATA%/Silevilence/mcp-api-gateway`，Linux/macOS 的 `~/.local/share` 或 `~/.config` 等）。
        - [ ] **自闭环元数据标准设计 (Metadata Schema Validation)**：
            - **系统级基础配置 (Global Settings)**：包含运行时监听端口、并发水位、日志存储策略等全局环境变量。
            - **多模态大模型凭证池 (LLM Provider Directory)**：提取已被引用的 AI 模型元数据，生成具备系统全局唯一性标识 (UUID/Hash-ID) 的配置节，供下游工具实例进行声明式关联绑定。
            - **项目级接口协议树 (API Gateway Profiles)**：涵盖 API 项目集结构、路由映射关系、入参转换Schema以及 MCP Tool 的行为元数据。
            - **运行态自校验**：确保导出的配置文件具备自闭环特性，`npx` 工具链在没有任何第三方外部依赖、仅持有此配置文件的状态下，可完全复现预期的网关及 MCP 路由拓扑。

## 🚧 开发中

- [ ] **多模态智能感知与视觉理解引擎集成**
    - [ ] 资产接入与底层服务配置
        - [ ] 新增“图像理解 (Vision Intelligence)” API 集类型选项
        - [ ] 集成统一大模型接入层 `ai-sdk`，实现异构模型供应商接口的高效适配与兼容
        - [ ] 建立全局基础设置空间 (Settings Page)（作为后续系统级配置的统一承载入口）
            - [ ] 供应商网关配置：支持配置异构通道（OpenAI Chat / Google Gemini / Anthropic Claude / Ollama），支持 BaseURL、APIKey 的配置与校验
            - [ ] 模型资产清单管理：支持单一供应商下多模型的级联注册与维护
            - [ ] 接入模型发现机制：支持基于供应商接口动态拉取模型列表（服务发现）或手动输入指定 Model ID
            - [ ] 模型物理特征标定：支持显式声明模型的核心能力画像（是否支持思维链/思考、是否支持多模态/视觉能力）
    - [ ] 多模态原子能力构建 (Core Vision Tools)
        - [ ] **前置公共规约与安全边界**：
            - 所有原子接口必须前置参数 schema 校验及边界防错机制。
            - **标准入参协议定义（如无特别声明，默认包含此结构）**：
                1. image (File/Buffer)：主分析目标图像
                2. prompt (string, Optional)：自定义提示词（缺省时注入场景默认提示词，用于精细化引导理解重点及输出约束）
                3. stream (boolean, Optional)：是否启用流式响应（默认：false）
            - **标准出参协议定义（如无特别声明，默认包含此结构）**：
                1. text (string)：模型推理输出的结构化文本
        - [ ] **ui_to_artifact (UI 视觉资产还原)**
            - **功能定义**：将 UI 视觉资源深度转化为前端代码、高保真提示词、系统级设计规范或多维自然语言描述，覆盖从设计稿到生产力落地的全生命周期。
            - **扩展入参**：
                1. type* (enum: Code | Prompt | Spec | Description)：目标产物生成策略
        - [ ] **ocr (智能文本提取)**
            - **功能定义**：基于大模型多模态视觉感知能力，提取并识别输入图像中的结构化文本信息，支持多语言与复杂布局排版。
        - [ ] **ui_diff_check (UI 视觉一致性稽查)**
            - **功能定义**：对比基准图与对比图之间的像素级、布局级差异，输出视觉偏差与缺陷报告，专注于 UI 质量保障 (QA) 与还原度验证。
            - **入参重构**（覆盖标准输入）：
                1. base_image* (File/Buffer)：设计基准图（必填，替代标准 image 字段）
                2. compare_image* (File/Buffer)：实现对比图（必填）
                3. prompt (string, Optional) & stream (boolean, Optional)
        - [ ] **image_analysis (通用图像解构)**
            - **功能定义**：泛化多模态图像感知能力，处理并解析非特定垂直场景下的日常通用视觉内容与复杂语境。
        - [ ] **video_analysis (视频动态场景分析)**
            - **功能定义**：支持 MP4/MOV/M4V 等主流格式（限制本地文件体积 ≤ 8MB）的视频解析，实现关键帧抽取、动态事件捕获与核心要点生成。
            - **入参重构**（覆盖标准输入）：
                1. video_file* (File/Buffer)：视频文件载荷（必填，无标准 image 输入）
                2. prompt (string, Optional) & stream (boolean, Optional)
    - [ ] 模型路由与多端透出
        - [ ] 动态模型路由分发：每个工具接口均支持按需绑定具体的执行模型（前置条件：该模型必须具有“支持视觉”特征）
        - [ ] 动态 MCP 注册映射：根据当前配置，将已激活的多模态原子接口无缝映射并暴露为标准的 MCP Tools 服务端点

## ✅ 已完成

- [x] **项目初始化**
    - [x] 基于 npm 构建前后端工程化基础骨架
    - [x] 梳理并配置 `.gitignore` 规则以规范版本控制范围
    - [x] 统一定义并配置核心 NPM Scripts 生命周期脚本
        - [x] dev：支持前后端服务并发启动及模块热替换 (HMR)
        - [x] build：基于 tsx 执行严格的静态类型检查与生产环境编译
        - [x] test：集成并执行全局自动化测试用例
    - [x] 运行 `build` 与 `test` 链路，验证工程基线状态与框架可用性

- [x] **核心架构与业务系统开发**
    - [x] 完成前后端基础骨架搭建及核心工作台开发
    - [x] 后端功能规约
        - [x] 确立路由隔离规范：内部控制面接口统一采用 `/internal` 前缀，外部暴露接口（预留）统一采用 `/api` 前缀
        - [x] 实现多源 API 资产接入与模型管理体系
            - 自定义接口声明模型（基础能力建立）
                - 全面支持标准 RESTful 请求方法（GET/POST/PUT/DELETE 等）
                - 接口入参结构化规约
                    - 载荷传输协议分类
                        - Query/Path 变量
                        - Request Body
                        - Form Data
                    - 字段元数据定义
                        - 字段标识 (Key)
                        - 数据类型约束 (int/string/date 等)
            - OpenAPI 规范对接模型（扩展解析能力）
                - 远程 URL 托管解析
                    - 支持基于源 URL 的元数据拉取与按需热同步
                - 本地 JSON 离线导入
                    - 支持标准 JSON 文件的解析与迭代覆盖上传
            - 架构级扩展支持（采用策略/适配器等设计模式抽象解析引擎，预留未来多协议接入空间）
    - [x] 前端视图与交互规约
        - [x] 确立应用访问策略：采用无鉴权单租户模式，应用级入口直达核心工作台视图
        - [x] 构建全局 API 资产管理看板
        - [x] 封装跨组件标准的 CRUD 业务交互行为
        - [x] 建立级联领域模型管理视图："API 项目集"（策略容器） -> "API 节点"
            - 针对自定义接口项目：开放完整数据表单编辑权限，支持 API 节点的全生命周期操作 (CRUD)
            - 针对 OpenAPI 托管项目：建立资源关联机制（支持配置远程 URL 同步或本地 JSON 迭代上传）
                - 变更同步机制：版本迭代时触发智能 Diff 比对，映射新旧节点并安全继承已有配置状态
                - 权限管控机制：底层接口元数据实现只读锁定，仅开放附加属性（如备注、隐藏状态）的覆写权限
            - 资产治理增强矩阵：支持跨节点的备注标注、逻辑分组 (Group) 划分及视图状态过滤（支持归档至隐藏池，需手动触发反归档）
            - 审计追踪体系：全量沉淀业务级操作日志，并应用 7 天滚动轮转清理策略 (Log Rotation)

- [x] **MCP 服务端引擎集成与接口开放体系构建**
    - [x] 引入并集成 TypeScript MCP Server SDK，基于 `streamableHttp` 协议建立 MCP 服务通信底座
    - [x] 构建统一的 API 与 MCP 双向能力透出机制
        - [x] API 资产管理网关规范落地
            - [x] 执行路由分层重构：将核心操作下沉暴露于 `/api` 命名空间，实现 Web 端与外部调用链路的代码复用与能力归一
            - [x] 部署全局流量控制策略：在网关层实施并发限流及防刷机制（14 QPS 阈值限额）
            - [x] 封装"API 项目集"标准生命周期管理接口 (CRUD)
            - [x] 封装"API 节点"标准生命周期管理接口 (CRUD)：强制校验关联项目 ID，并针对 OpenAPI 类型资产实施权限收敛（锁定基础元数据防篡改，仅开放附加属性覆写权限）
        - [x] MCP 工具链 (Tooling) 映射与适配
            - [x] 确立 MCP 服务通信网关端点规范：`/api/mcp`
            - [x] 基于底层 API 治理接口，横向封装并注册标准 MCP 操作工具 (Tools) 供外部模型侧调用
        - [x] 完善全域流量审计机制：沉淀统一的接入层访问日志 (Access Log)，并执行 7 天日志轮转清理策略 (Log Rotation)

- [x] **用户体验 (UX) 与视觉 (UI) 规范升级**
    - [x] 确立全局视觉风格：采用暗黑模式 (Dark Mode) 与极简科技感设计语言，重塑高质感的界面视觉呈现
    - [x] 优化核心交互链路：降低操作路径的认知负荷，确保全量业务场景的交互响应丝滑与流转顺畅

- [x] **API 治理架构升级与扩展**
    - [x] 构建本地化数据持久层
        - [x] 统一业务数据（API 项目集、接口元数据、审计日志等）至本地 `.data` 目录进行落盘存储与生命周期管理
    - [x] 建立标准化的资源唯一标识 (Slug/Alias) 体系（正则约束：仅限英文字母、数字、短横线及下划线）
        - [x] 定义项目级标识：在全局 API 项目空间内强制唯一，作为路由与服务发现的基础
        - [x] 定义接口级标识：限定于所属 API 项目作用域内唯一，作为工具化转换的基准命名
    - [x] 研发 API 在线调试与沙箱验证模块
        - [x] 基于接口元数据规范，动态渲染请求参数组装表单
        - [x] 封装底层 HTTP/REST 发起引擎，实时采集响应载荷 (Payload) 与链路耗时 (RT)
            - 集成代码高亮与格式化组件，实现 JSON/XML 等响应报文的结构化美观渲染
    - [x] MCP 生态集成与动态服务网关演进
        - [x] 支持项目粒度的 MCP Server 隔离部署（基于 `streamableHttp` 协议建立独立服务）
            - 确立项目级路由分发规范：端点映射为 `/api/{项目标识}/mcp`
            - 设立前置校验屏障：拦截未配置“项目标识”的开启请求
            - 沉淀工具化转换机制：支持批量勾选已配置“接口标识”的 API，动态注册为 MCP Tool（Tool Name 映射为接口标识，Schema 映射为接口参数元数据）
            - 统一调用底座：Tool 执行链路直接复用“在线调试”模块的底层 HTTP 请求引擎，确保行为一致性
        - [x] 扩展主控 MCP (`/api/mcp`) 注册中心能力：注入全局服务发现工具 (Service Discovery Tool)，用于动态下发当前已激活的项目级 MCP 节点列表及路由寻址信息

- [x] **文件系统集成适配器支持**
    - [x] 资产接入与根路径配置
        - [x] 新增“文件系统 (File System)” API 集类型选项
        - [x] 实现工作区根目录 (Workspace Root) 配置项，集成交互式路径选择器 (Path Picker) 以辅助路径录入
    - [x] 文件系统核心原子能力构建 (Capability Matrix)
        - [x] **glob (高性能模式匹配)**
            - **功能定义**：跨规模代码库的文件模式匹配引擎。支持标准 Glob 语法（如 `**/*.js`）；按修改时间倒序返回绝对路径；支持深度检索及相对/绝对路径映射；单次检索限制 100 个文件；强制作用域校验，确保路径解析不超出预设工作区根目录。
            - **入参规约**：
                1. pattern* (string)：匹配模式，用于定位目标文件
                2. path (string)：检索起始绝对路径，缺省值为工作区根目录
        - [x] **ls (目录结构拓扑检索)**
            - **功能定义**：指定路径的层级结构平铺与检索。返回树状拓扑并标注类型图标；支持深度递归检索（最大 5 层）；内置黑名单过滤机制（排除 `node_modules`, `dist`, `.git` 及常规隐藏文件，`.env.example` 除外）；结果集上限 100 条；强制根路径作用域校验。
            - **入参规约**：
                1. path (string)：检索目标绝对路径，缺省值为工作区根目录
                2. recursive (boolean)：是否开启递归检索（默认：false）
        - [x] **grep (正则内容检索)**
            - **功能定义**：基于正则表达式的全量内容检索工具。支持标准正则语法及文件类型过滤（include 模式）；返回包含匹配内容的绝对路径与行号；单次匹配限额 100 条；自动跳过二进制文件并排除冗余目录（node_modules 等）；强制根路径作用域校验。
            - **入参规约**：
                1. pattern* (string)：用于匹配文件内容的正则表达式
                2. path (string)：检索起始绝对路径，缺省值为工作区根目录
                3. include (string)：文件范围过滤器（如 `*.{ts,tsx}`）
        - [x] **read (安全文件读取)**
            - **功能定义**：受控的工作区文件内容读取。强制执行工作区作用域校验；支持大文件分页加载（默认读取前 2000 行）；内置行超长截断保护；自动识别并拦截二进制文件；返回带行号的内容结构。
            - **入参规约**：
                1. file_path* (string)：目标文件绝对路径
                2. offset (number)：起始行号（从 1 开始计）
                3. limit (number)：读取行数上限（默认 2000 行）
        - [x] **edit (原子级内容替换)**
            - **功能定义**：基于精确字符串匹配的内容补丁工具。执行前强制要求至少调用一次 `read` 接口以获取上下文；支持缩进保留及多点替换 (replace_all)；内置失败保护机制（若 old_string 未匹配、匹配不唯一且未开启 replace_all、或新旧内容一致则抛出异常）；严禁修改行号前缀。
            - **入参规约**：
                1. file_path* (string)：目标文件绝对路径
                2. old_string* (string)：待替换的原始文本
                3. new_string* (string)：目标替换文本
                4. replace_all* (boolean)：是否执行全局替换（默认：false）
        - [x] **write (全量内容覆盖)**
            - **功能定义**：资源全量写入与初始化。若文件已存在则执行覆盖；操作前置条件为已调用 `read` 确认目标状态；支持父级目录自动递归创建；强制执行工作区作用域校验；推荐在修改场景优先使用 `edit` 接口。
            - **入参规约**：
                1. file_path* (string)：目标文件绝对路径
                2. content* (string)：写入的全量文本内容
        - [x] **delete (级联资源销毁)**
            - **功能定义**：文件或目录的物理删除操作。**注意：操作具备不可逆性。** 支持文件直接删除及目录级联删除（需显式开启 recursive）；执行前强制路径校验及工作区范围限制，防止非预期数据丢失。
            - **入参规约**：
                1. path* (string)：待删除的文件或目录绝对路径
                2. recursive (boolean)：针对目录是否执行递归删除（默认：false）
    - [x] 交互体验与安全合规升级
        - [x] 安全警示体系：针对高危操作（delete, write, edit）在 UI 侧建立显著的视觉标识与二次确认机制
    - [x] MCP 动态能力映射
        - [x] 根据 API 集已激活的原子接口，动态注册并暴露相应的 MCP Tools
        - [x] 确保 MCP Tool 描述与 API 定义保持同步，并遵循对应的参数约束规范
