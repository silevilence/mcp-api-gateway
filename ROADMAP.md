# 项目开发路线图 (Roadmap)

## 📅 计划中

## 🚧 开发中

- [ ] **项目初始化**
    - [ ] 基于 npm 构建前后端工程化基础骨架
    - [ ] 梳理并配置 `.gitignore` 规则以规范版本控制范围
    - [ ] 统一定义并配置核心 NPM Scripts 生命周期脚本
        - [ ] dev：支持前后端服务并发启动及模块热替换 (HMR)
        - [ ] build：基于 tsx 执行严格的静态类型检查与生产环境编译
        - [ ] test：集成并执行全局自动化测试用例
    - [ ] 运行 `build` 与 `test` 链路，验证工程基线状态与框架可用性

- [ ] **核心架构与业务系统开发**
    - [ ] 完成前后端基础骨架搭建及核心工作台开发
    - [ ] 后端功能规约
        - [ ] 确立路由隔离规范：内部控制面接口统一采用 `/internal` 前缀，外部暴露接口（预留）统一采用 `/api` 前缀
        - [ ] 实现多源 API 资产接入与模型管理体系
            - 自定义接口声明模型（基础能力建立）
                - 全面支持标准 RESTful 请求方法（GET/POST/PUT/DELETE 等）
                - 接口入参结构化规约
                    - 载荷传输协议分类
                        - Query/Path 变量
                        - Request Body
                        - Form Data
                    - 字段元数据定义
                        - 字段标识 (Key)
                        - 数据类型约束 (int/string/date 等)
            - OpenAPI 规范对接模型（扩展解析能力）
                - 远程 URL 托管解析
                    - 支持基于源 URL 的元数据拉取与按需热同步
                - 本地 JSON 离线导入
                    - 支持标准 JSON 文件的解析与迭代覆盖上传
            - 架构级扩展支持（采用策略/适配器等设计模式抽象解析引擎，预留未来多协议接入空间）
    - [ ] 前端视图与交互规约
        - [ ] 确立应用访问策略：采用无鉴权单租户模式，应用级入口直达核心工作台视图
        - [ ] 构建全局 API 资产管理看板
        - [ ] 封装跨组件标准的 CRUD 业务交互行为
        - [ ] 建立级联领域模型管理视图："API 项目集"（策略容器） -> "API 节点"
            - 针对自定义接口项目：开放完整数据表单编辑权限，支持 API 节点的全生命周期操作 (CRUD)
            - 针对 OpenAPI 托管项目：建立资源关联机制（支持配置远程 URL 同步或本地 JSON 迭代上传）
                - 变更同步机制：版本迭代时触发智能 Diff 比对，映射新旧节点并安全继承已有配置状态
                - 权限管控机制：底层接口元数据实现只读锁定，仅开放附加属性（如备注、隐藏状态）的覆写权限
            - 资产治理增强矩阵：支持跨节点的备注标注、逻辑分组 (Group) 划分及视图状态过滤（支持归档至隐藏池，需手动触发反归档）
            - 审计追踪体系：全量沉淀业务级操作日志，并应用 7 天滚动轮转清理策略 (Log Rotation)

- [ ] **MCP 服务端引擎集成与接口开放体系构建**
    - [ ] 引入并集成 TypeScript MCP Server SDK，基于 `streamableHttp` 协议建立 MCP 服务通信底座
    - [ ] 构建统一的 API 与 MCP 双向能力透出机制
        - [ ] API 资产管理网关规范落地
            - [ ] 执行路由分层重构：将核心操作下沉暴露于 `/api` 命名空间，实现 Web 端与外部调用链路的代码复用与能力归一
            - [ ] 部署全局流量控制策略：在网关层实施并发限流及防刷机制（14 QPS 阈值限额）
            - [ ] 封装"API 项目集"标准生命周期管理接口 (CRUD)
            - [ ] 封装"API 节点"标准生命周期管理接口 (CRUD)：强制校验关联项目 ID，并针对 OpenAPI 类型资产实施权限收敛（锁定基础元数据防篡改，仅开放附加属性覆写权限）
        - [ ] MCP 工具链 (Tooling) 映射与适配
            - [ ] 确立 MCP 服务通信网关端点规范：`/api/mcp`
            - [ ] 基于底层 API 治理接口，横向封装并注册标准 MCP 操作工具 (Tools) 供外部模型侧调用
        - [ ] 完善全域流量审计机制：沉淀统一的接入层访问日志 (Access Log)，并执行 7 天日志轮转清理策略 (Log Rotation)

## ✅ 已完成
